# Contributing

Feel free to fork the repository and submit pull requests.
Ensure `make all` passes successfully before opening a PR.

- Code requires strict type checking (`mypy`).
- Lint with `ruff`.
